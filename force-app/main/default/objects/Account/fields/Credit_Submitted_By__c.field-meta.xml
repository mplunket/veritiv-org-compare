<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Credit_Submitted_By__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>This is the user that is submitting the data to Credit.  Sales Support will most likely initiate the process, so we need to save that user for emails down the line.</description>
    <externalId>false</externalId>
    <label>Credit - Submitted By</label>
    <referenceTo>User</referenceTo>
    <relationshipName>Accounts2</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>

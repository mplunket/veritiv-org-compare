<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Approver_Reject_validation</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
				OR(
								$Profile.Name =  &apos;Sales Admin - Lightning&apos; ,
		      $Profile.Name =  &apos;Sales Admin Restricted - Lightning&apos;
				),
				OR(
								ISCHANGED(Actioned_By__c),
		      ISCHANGED(Actioned_Date__c),
		      ISCHANGED(Status__c)
				)
)</errorConditionFormula>
    <errorMessage>You do not have access to take this action.</errorMessage>
</ValidationRule>

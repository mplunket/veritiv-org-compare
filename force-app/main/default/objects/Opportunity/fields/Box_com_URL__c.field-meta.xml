<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Box_com_URL__c</fullName>
    <description>Workfront passes this field to the Engineered Project object, will use a workflow to copy it to Opportunity, so we can pick it up on a PIM work order that is related to the opportunity</description>
    <externalId>false</externalId>
    <label>Box.com URL</label>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Url</type>
</CustomField>
